# Define global variables
variables:
- group: 'terraform-backend'
- group: 'terraform-system'
- group: '${{ variables.Environment }}'
- name: 'backendAzureRmContainerName'
  value: $[lower(variables['Service'])]